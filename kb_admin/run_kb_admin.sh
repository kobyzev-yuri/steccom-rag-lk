#!/bin/bash
# KB Admin Launcher
# Скрипт запуска системы управления базами знаний

echo "🧠 Запуск KB Admin - Система управления базами знаний"
echo "=================================================="

# Переходим в корневую директорию проекта
cd "$(dirname "$0")/.."

# Проверка виртуального окружения
if [ ! -d ".venv" ]; then
    echo "❌ Виртуальное окружение не найдено. Создайте .venv в корне проекта."
    exit 1
fi

# Активация виртуального окружения
echo "🔧 Активация виртуального окружения..."
source .venv/bin/activate

# Устанавливаем PYTHONPATH для доступа к корневым модулям
export PYTHONPATH=".:$PYTHONPATH"

echo "🚀 Запуск Streamlit приложения..."
streamlit run kb_admin/app.py --server.port 8502 --server.address 0.0.0.0
